---
import Links from "./Links";

type Props = {
  steamId: string;
  gameId: string;
};

const { props } = Astro;
---

<div class="grid w-full grid-cols-2 gap-2 sm:grid-cols-3 md:grid-cols-4">
  <Links steamId={props.steamId} gameId={props.gameId} client:only />
</div>
